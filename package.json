{
  "name": "hono-start",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "practice": "bun run --hot src/practice",
    "build": "bun build src/index.ts --outdir=public"
  },
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
